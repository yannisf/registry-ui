# Client for the registry application

## Initialize
    npm install
    bower install

## Setup environment path
    . env.sh    (linux)
    env.cmd   (windows)

## Build
    gulp
    mvn clean install
